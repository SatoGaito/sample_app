<h1>編集画面</h1>
<%= form_for(@list, url:"/todolists/#{@list.id}") do |f| %>
  <h4>タイトル</h4>
  <p><%= f.text_field :title %></p>
  <h4>本文</h4>
  <p><%= f.text_area :body %></p>
  <%= f.submit '保存' %>
<% end %>